% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dp-control.R
\name{dp_control}
\alias{dp_control}
\title{Differential-privacy controls (Track B)}
\usage{
dp_control(
  epsilon,
  delta = 0,
  unit = c("person", "row"),
  max_rows_per_person = NULL,
  mechanism = c("laplace", "gaussian"),
  dependence = c("tree", "independent"),
  structure_frac = NULL,
  cross_table = FALSE,
  longitudinal = FALSE,
  baseline = NULL,
  transition_order = 1L,
  transition_cross = 0L,
  transition_parent = 0L,
  bins = 12L,
  bounds = NULL,
  domain = c("dp", "public", "data"),
  domain_frac = 0.1
)
}
\arguments{
\item{epsilon}{Positive privacy-loss budget. Smaller = more private, less
utility.}

\item{delta}{Failure probability for approximate DP; \code{0} requests pure
\eqn{\epsilon}-DP (only the Laplace mechanism supports \code{delta = 0}).}

\item{unit}{Privacy unit. \code{"person"} (default) protects an individual across
all their rows; \code{"row"} is weaker and cheaper (each row independent).}

\item{max_rows_per_person}{Cap on how many rows one person may contribute,
used to bound sensitivity at \code{unit = "person"}. \code{NULL} (default) uses \code{1}
(each person contributes at most one row); rows beyond the cap are dropped
by per-person subsampling before measuring. Set this from public domain
knowledge when a person legitimately has several rows. For a longitudinal DP
release (a \code{structure} with a nesting index), this must be \verb{>= 2} — it caps
the transition sensitivity — and the per-person prefix of that many rows is
kept in temporal order. For a linked DP release (\code{\link[=synth_linked]{synth_linked()}}) it is the
maximum children kept per parent — a single integer applied to every child
table, or a \strong{named} vector/list keyed by child-table name (e.g.
\code{list(admissions = 8, labs = 20)}) — and the root cap is always 1.}

\item{mechanism}{Noise mechanism: \code{"laplace"} (pure \eqn{\epsilon}-DP) or
\code{"gaussian"} (approximate DP with zCDP composition; needs \code{delta > 0}).}

\item{dependence}{Dependence structure of the generative model: \code{"tree"}
(default; a Chow-Liu tree over pairwise marginals) or \code{"independent"}
(one-way marginals only).}

\item{structure_frac}{Budget-efficient structure learning for the flat
\code{dependence = "tree"} release (\code{\link[=synth]{synth()}} with three or more variables).
\code{NULL} (default) measures all \eqn{\binom{d}{2}} pairwise marginals at full
fidelity and reuses them as both the Chow-Liu weights and the tree's
conditionals. A number in \verb{(0, 1)} instead spends only that fraction of the
marginal budget on a cheap all-pairs scan used \strong{solely} to select the tree,
then concentrates the remaining \code{1 - structure_frac} on re-measuring just the
\eqn{d - 1} chosen edges (plus the \code{d} one-way marginals) — so the budget
lands on the parameters that survive into the model instead of on pairs that
are discarded. Structure selection tolerates noise well, so a small slice
(e.g. \code{0.2}–\code{0.3}) usually sharpens the retained edges, more so as the number
of variables grows. Both passes compose into the same exact
(\eqn{\epsilon}, \eqn{\delta}) budget. Inert for \code{dependence = "independent"},
for fewer than three variables (the tree is then trivial), and for
longitudinal / linked DP releases (which keep the single-pass fitter).}

\item{cross_table}{Linked DP only (\code{\link[=synth_linked]{synth_linked()}}). When \code{TRUE}, a child
table's variables are conditioned on the \strong{synthetic parent's} attributes:
for each child table with a modellable immediate parent, parent-by-child
joint marginals are measured (at the child's per-entity path-cap sensitivity,
the same as a child one-way marginal) and folded into the child's Chow-Liu
structure as fixed context nodes, so each child variable's single strongest
predictor may be a parent variable or another child variable. The synthetic
parent's already-drawn value then conditions the child draw, so cross-table
statistical dependence — not just referential integrity — survives the noise.
Costs extra budget (the added joints compose into the same
(\eqn{\epsilon}, \eqn{\delta})); \code{FALSE} (default) keeps child variables on
their own within-table marginals. Ignored by flat / longitudinal \code{synth()}
releases, which have no parent table.}

\item{longitudinal}{Linked DP only (\code{\link[=synth_linked]{synth_linked()}}). Model a child table's
repeated rows as a within-unit time series under DP, instead of exchangeable
records: for such a table the children-per-parent count model doubles as a
trajectory-length model, an initial-state model is measured over each unit's
first (temporally earliest) child row, and a first-order Markov transition
matrix \eqn{P(v_t \mid v_{t-1})} is measured per variable over consecutive
within-unit rows (ordered by the child's own key index). Sensitivities differ
by histogram — initial marginals at the parent's path cap, transitions at
\code{path_cap[parent] * (branching_cap - 1)} — and fold into the same exact
(\eqn{\epsilon}, \eqn{\delta}) budget. Pass \code{TRUE} to model every eligible
child table (a child with a branching cap \eqn{\ge} 2 and at least one
variable), or a character vector of child-table names to model only those.
The child's branching cap in \code{max_rows_per_person} bounds the transition
sensitivity, so it must be \eqn{\ge} 2 for a named table, and the prefix of
that many rows is kept in temporal order. Setting \code{cross_table = TRUE}
together with a longitudinal model on the same table \strong{combines} them: the
table's initial-state model is cross-conditioned on the synthetic parent
(adding the parent-by-child joints at the same first-row sensitivity as the
initial marginals), and the transition chain then carries that parent
dependence across the trajectory — the parent shapes where a trajectory
starts, while the transitions stay parent-free, so the only extra cost is the
initial-state joints. \code{FALSE} (default) treats child rows as exchangeable.
Ignored by flat / longitudinal \code{synth()} releases (which pick the DP Markov
engine straight from the \code{structure} formula).}

\item{baseline}{Longitudinal releases. Names of \strong{subject-invariant} columns —
baseline covariates that do not change across a unit's rows (e.g. birth sex, a
baseline measurement). These are held \strong{exactly constant} within each
synthetic unit: they are modelled once in the initial-state model (so their
joint distribution and their correlation with the first visit are preserved)
and then broadcast to every row, rather than being stepped through a
transition matrix that would let them drift. Declaring a column baseline is
public schema knowledge, so it costs no budget; it also \strong{removes} that
column's transition histogram from the release, sharpening every remaining
measurement at the same (\eqn{\epsilon}, \eqn{\delta}). Applies to a
longitudinal \code{synth()} release (a \code{structure} with a nesting index) and, per
table, to any \strong{longitudinally-modelled linked child} (the names are matched
against each such child's own columns). \code{NULL} (default) treats every column
as time-varying. Names that match no modelled column are ignored. Ignored by a
flat \code{synth()} release (one row per unit) and by non-longitudinal linked
child tables.}

\item{transition_order}{Longitudinal releases. Markov order of the within-unit
transition model: how many of a variable's \strong{own} immediately preceding
values condition its next value. \code{1} (default) is the first-order model
\eqn{P(v_t \mid v_{t-1})}; \code{2} gives \eqn{P(v_t \mid v_{t-1}, v_{t-2})}, and so
on. Higher orders capture momentum but need a larger row cap: the order must be
at most \code{max_rows_per_person - 1} — for a linked release, one less than the
\strong{branching cap} of each longitudinally-modelled child (validated per table).
A unit then contributes at most \code{cap - order} transition tuples per variable,
so the transition sensitivity — and hence the noise — actually \emph{drops} with
order, at the price of a finer conditioning grid (sparser cells) and of not
modelling the earliest rows' own dynamics separately: rows before position
\code{order + 1} are generated by marginalising the same measured tensor, which is
post-processing and costs no budget. Applies to a longitudinal \code{synth()}
release and, uniformly, to every longitudinally-modelled linked child; ignored
by flat releases and non-longitudinal linked children.}

\item{transition_cross}{Longitudinal releases. Number of \strong{other} variables
(each at lag 1) that additionally condition each variable's transition —
moving from \eqn{P(v_t \mid v_{t-1}, \dots)} to also conditioning on
\eqn{u_{t-1}} for the \code{transition_cross} most strongly associated companions
\code{u}. Cross-parents are selected automatically and budget-neutrally from the
pairwise marginals the Chow-Liu tree already measures (contemporaneous mutual
information, a leak-free proxy for lag-1 cross-predictiveness) — for a linked
child that means its own within-child pairwise marginals — so
\code{transition_cross > 0} requires \code{dependence = "tree"}. Adding conditioning
columns does \strong{not} change the (\eqn{\epsilon}, \eqn{\delta}) budget — a
transition tuple still lands in exactly one cell — it trades budget-free
structure for cell sparsity, so keep it small. \code{0} (default) conditions each
variable on its own past only. Applies to a longitudinal \code{synth()} release and
to every longitudinally-modelled linked child; ignored by flat releases and
non-longitudinal linked children.}

\item{transition_parent}{Linked DP only (\code{\link[=synth_linked]{synth_linked()}}), longitudinal
children. Number of \strong{immediate-parent} attributes that additionally
condition each time-varying child variable's transition. Today
\code{cross_table = TRUE} cross-conditions only a longitudinal child's
\emph{initial state} on the synthetic parent; the parent's influence then rides
the own-lag chain and decays. \code{transition_parent = p} instead re-injects the
(subject-invariant) parent attributes into the transition tensor at \strong{every}
step — moving from \eqn{P(v_t \mid v_{t-1}, \dots)} to also conditioning on
the parent's \eqn{w} for the \code{p} parent attributes \code{w} most strongly
associated with \code{v} — so parent \eqn{\to} child dependence stays anchored
across the whole trajectory rather than washing out. The parents are selected
automatically and \strong{budget-neutrally} from the parent-by-child joints that
the cross-conditioned initial state already measures, so it costs nothing in
the (\eqn{\epsilon}, \eqn{\delta}) budget (a transition tuple still lands in
exactly one cell — same sensitivity, same histogram count), trading
budget-free structure for cell sparsity. Because it reuses those joints,
\code{transition_parent > 0} \strong{requires \code{cross_table = TRUE}} for the child (an
error is raised otherwise). \code{0} (default) leaves the transitions parent-free
(the initial-state cross-conditioning is unaffected). Composes with
\code{baseline}, \code{transition_order} and \code{transition_cross}. Ignored by flat
\code{synth()} releases and non-longitudinal linked children, which have no parent
trajectory to condition.}

\item{bins}{Number of equal-width bins used to discretise each numeric
variable (default 12). Finer grids sharpen one-way marginals but make the
noisy two-way marginals used by \code{dependence = "tree"} weaker per cell, so a
moderate value usually gives the best correlation fidelity. Means and sums
are largely unaffected (bin contents are decoded uniformly within the bin).}

\item{bounds}{Optional named list giving \code{c(lower, upper)} for numeric
variables, used as public, data-independent bin edges. How variables \emph{not}
named here are handled depends on \code{domain}.}

\item{domain}{How numeric bin edges are chosen for variables without a public
range in \code{bounds}: \code{"dp"} (default) estimates them under differential privacy
and accounts for the cost; \code{"public"} requires \code{bounds} for every numeric
variable (error otherwise) and spends no budget on the domain; \code{"data"} reads
them from the data range with a warning and \emph{excludes} that step from the
accounting (non-rigorous; benchmarking only).}

\item{domain_frac}{Fraction of the privacy budget spent estimating bin edges
under \code{domain = "dp"} (default \code{0.1}). Split evenly across the two
quantile queries per estimated variable and composed with the marginal
measurements, so the total spend is exactly (\eqn{\epsilon}, \eqn{\delta}).
Ignored unless some numeric variable actually needs estimating.}
}
\value{
An object of class \code{dp_control} (a validated list).
}
\description{
Opt into differentially private synthesis. Passing the result as
\code{synth(..., privacy = dp_control(...))} selects the DP synthesiser and yields
a formal (\eqn{\epsilon}, \eqn{\delta}) guarantee. The privacy unit is
\strong{person-level} by default: the guarantee protects a whole individual (all of
their rows), enforced by bounding each person's contribution before any budget
is spent.
}
\details{
The DP engine (Track B) is a \emph{marginal-based} synthesiser in the
PrivBayes / MST lineage. Continuous variables are discretised into a public
grid; low-order marginals are measured under the chosen noise mechanism with
correct budget composition; and synthetic records are drawn from the resulting
model. With \code{dependence = "tree"} a Chow-Liu tree of pairwise dependencies is
learned from the same noisy marginals (no extra budget) so second-order
structure is retained; \code{dependence = "independent"} keeps only one-way
marginals. All noise calibration and composition is reported back on the
result (see the accounting printed by \code{\link[=synth]{synth()}}).

Where the bin edges come from matters, because reading them from the data (its
min / max) is itself a data-dependent step that can leak an individual's
presence. \code{domain} controls this:
\describe{
\item{\code{"dp"} (default)}{Rigorous and automatic. Numeric variables named in
\code{bounds} use those public edges at no cost; any other numeric variable has
its working range \strong{estimated under differential privacy} (a clamp-free
exponential-mechanism quantile at each end), and that estimation spends an
accounted slice \code{domain_frac} of the budget. The reported
(\eqn{\epsilon}, \eqn{\delta}) is therefore exact end to end — discretisation
adds no unaccounted leakage.}
\item{\code{"public"}}{Fully data-independent and free: every numeric variable
\strong{must} be given a public range in \code{bounds} (an error is raised otherwise)
and no budget is spent on the domain. The recommended mode when public
ranges (codebook / physiological limits) are available.}
\item{\code{"data"}}{The non-rigorous legacy behaviour: bin edges are taken from
the data range with a warning, and that step is \strong{excluded} from the
accounting. Kept only for benchmarking; do not use for a governed release.}
}
Categorical variables carry their domain in their type: \code{factor} and \code{logical}
columns use their declared levels (public metadata, no leakage). In the \code{"dp"}
and \code{"public"} modes a bare \code{character} column is refused — convert it to a
\code{factor} with its full \code{levels} so the category set is public.
}
\examples{
dp <- dp_control(epsilon = 1, delta = 1e-6, mechanism = "gaussian")
dp
}
