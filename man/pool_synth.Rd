% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pool.R
\name{pool_synth}
\alias{pool_synth}
\title{Pooled inference from synthetic data}
\usage{
pool_synth(
  object,
  analysis,
  rule = c("synthpop", "reiter"),
  population_inference = TRUE,
  conf.level = 0.95,
  ...
)
}
\arguments{
\item{object}{A \code{synth_result} from \code{\link[=synth]{synth()}} (ideally with \code{m >= 5}).}

\item{analysis}{A function of one synthetic \code{data.frame} returning a fitted
model (with \code{coef()}/\code{vcov()}) or a \code{list(estimate =, variance =)}.}

\item{rule}{Combining rule: \code{"synthpop"} (default, Raab/Nowok/Dibben 2016) or
\code{"reiter"} (Reiter 2003 fully-synthetic; needs \code{m >= 2}).}

\item{population_inference}{If \code{TRUE} (default) the standard error targets the
population the real data was drawn from, inflating for synthesis. If \code{FALSE}
it reports only the (size-rescaled) within-synthesis variance — the standard
error an analyst of the original data would have seen.}

\item{conf.level}{Confidence level for the intervals (default \code{0.95}).}

\item{...}{Reserved for future use.}
}
\value{
A \code{flexsynth_pool} object; \verb{$estimates} is a data frame with \code{term},
\code{estimate}, \code{std.error}, \code{statistic}, \code{p.value}, \code{conf.low}, \code{conf.high}.
}
\description{
Fit an analysis on each of the \code{m} synthetic datasets in a \code{\link[=synth]{synth()}} result
and combine the results into one estimate whose standard error reflects the
extra variability that synthesis introduces. An analysis of a single
synthetic dataset does not estimate between-synthesis variation, so its usual
standard errors can be miscalibrated. These functions implement published
fully-synthetic combining rules; their large-sample calibration still depends
on the estimand, synthesis model, sample size, and analysis assumptions.
}
\details{
\code{analysis} is run once per synthetic dataset and must return either a fitted
model supporting \code{coef()} and \code{vcov()} (such as \code{\link[=lm]{lm()}} or \code{\link[=glm]{glm()}}) or a list
with numeric \code{estimate} and \code{variance} (squared standard error) vectors. The
per-dataset estimates are then combined with the chosen \code{rule}: \code{"synthpop"}
(Raab, Nowok & Dibben 2016, matching synthpop's \code{summary.fit.synds}) or
\code{"reiter"} (Reiter 2003 fully-synthetic, \verb{Tf = (1 + 1/m) b_m - vbar}, which
needs \code{m >= 2} and can be negative). Intervals are large-sample normal, as in
synthpop.

Only Track A (\code{\link[=synth]{synth()}} without \code{privacy}) results are supported: differentially
private (Track B) inference must additionally account for the DP noise and is
out of scope here. Pooling of a \code{\link[=synth_linked]{synth_linked()}} result is not yet supported;
analyse a single table's synthetic frame instead.
}
\examples{
d <- data.frame(id = 1:400, x = rnorm(400))
d$y <- 1 + 2 * d$x + rnorm(400)
res <- synth(d, ~ id, m = 5, seed = 1)
pool_synth(res, function(dat) lm(y ~ x, dat))
}
\seealso{
\code{\link[=synth_glm]{synth_glm()}} for the common linear / generalised-linear case.
}
