% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/constraints.R
\name{rule}
\alias{rule}
\title{Declare a synthesis constraint}
\usage{
rule(expr, scope = c("row", "unit"), label = NULL)
}
\arguments{
\item{expr}{A logical expression over the column names, e.g. \code{dbp <= sbp}.
With \code{scope = "row"} it is evaluated across a unit's rows and must be \code{TRUE}
for every row. With \code{scope = "unit"} it is evaluated once per unit (the
columns are that unit's rows in positional order) and must return \code{TRUE} —
use this for temporal logic such as \code{all(diff(los) >= 0)}. Missing (\code{NA})
results are treated as satisfied.}

\item{scope}{\code{"row"} (default) or \code{"unit"}.}

\item{label}{Optional human-readable label; defaults to the deparsed
expression.}
}
\value{
A \code{flexsynth_rule} object.
}
\description{
Capture a logical rule the synthetic data must satisfy. Rules are enforced by
\code{\link[=synth]{synth()}} via rejection sampling at the unit grain (see \emph{Details}).
}
\details{
The expression is written in terms of the data's column names and is \strong{not}
evaluated when \code{rule()} is called — it is captured and checked later against
the synthetic data.
}
\examples{
# A row rule and a within-unit temporal rule.
rule(dbp <= sbp)
rule(all(diff(visit_time) > 0), scope = "unit")
}
