% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/check-linkage.R
\name{check_linkage}
\alias{check_linkage}
\title{Check referential integrity of linked tables}
\usage{
check_linkage(object, keys = NULL, verbose = TRUE)
}
\arguments{
\item{object}{A \code{synth_linked_result}, or a named list of \code{data.frame}s.}

\item{keys}{When \code{object} is a raw list of tables, a named list of key
columns (as passed to \code{\link[=synth_linked]{synth_linked()}}). Ignored for a result object.}

\item{verbose}{Logical; print a short summary. Defaults to \code{TRUE}.}
}
\value{
Invisibly, a report \code{data.frame} (one row per table, or per table and
collection when \code{m > 1}) with an \code{ok} attribute that is \code{TRUE} when there
are no duplicate keys and no orphan rows.
}
\description{
Verify that a set of linked tables is internally consistent: every table's
key identifies its rows uniquely, and every child row's foreign key resolves
to an existing parent row (no orphans). Works on a \code{\link[=synth_linked]{synth_linked()}} result or
on a raw named list of tables (supply \code{keys} in that case).
}
\examples{
tabs <- list(
  patients   = data.frame(id = 1:3),
  admissions = data.frame(id = c(1, 2, 9), admission_id = c(1, 1, 1))
)
check_linkage(tabs, keys = list(patients = "id",
                                admissions = c("id", "admission_id")))
}
