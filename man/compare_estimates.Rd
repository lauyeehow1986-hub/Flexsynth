% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pool.R
\name{compare_estimates}
\alias{compare_estimates}
\title{Specific-utility comparison of an analysis on real vs synthetic data}
\usage{
compare_estimates(
  real,
  syn,
  analysis,
  rule = c("synthpop", "reiter"),
  population_inference = TRUE,
  conf.level = 0.95,
  ...
)
}
\arguments{
\item{real}{The real \code{data.frame}.}

\item{syn}{A \code{synth_result} (pooled) or a synthetic \code{data.frame}.}

\item{analysis}{A function of one \code{data.frame} returning a fitted model
(\code{coef()}/\code{vcov()}) or a \code{list(estimate =, variance =)}.}

\item{rule, population_inference}{Passed to \code{\link[=pool_synth]{pool_synth()}} when \code{syn} is a
multi-dataset result.}

\item{conf.level}{Confidence level for both intervals (default \code{0.95}).}

\item{...}{Reserved.}
}
\value{
A \code{flexsynth_utility} object; \verb{$estimates} has \code{term}, \code{est_real},
\code{est_syn}, \code{overlap}, \code{std_diff}, and both intervals.
}
\description{
Fit the same analysis on the real data and on the synthetic data and compare
the estimates. This is the \emph{analysis-specific} utility measure — "does my
regression come out the same?" — complementing the general marginal / pMSE
diagnostics of \code{\link[=diagnose]{diagnose()}}. For each term it reports the confidence-interval
overlap (Karr et al. 2006; 1 = identical intervals, 0 = just touching,
negative = disjoint) and the standardised difference of the estimates on the
real standard-error scale (smaller is better).
}
\details{
When \code{syn} is a multi-dataset \code{\link[=synth]{synth()}} result its estimates are pooled with
\code{\link[=pool_synth]{pool_synth()}} (so the synthetic standard errors reflect synthesis); a single
synthetic \code{data.frame} is analysed directly. \code{analysis} uses the same contract
as \code{\link[=pool_synth]{pool_synth()}}.
}
\examples{
d <- data.frame(id = 1:500, x = rnorm(500))
d$y <- 1 + 2 * d$x + rnorm(500)
res <- synth(d, ~ id, m = 5, seed = 1)
compare_estimates(d, res, function(dat) lm(y ~ x, dat))
}
\seealso{
\code{\link[=pool_synth]{pool_synth()}}, \code{\link[=diagnose]{diagnose()}}.
}
