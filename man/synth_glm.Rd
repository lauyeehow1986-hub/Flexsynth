% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pool.R
\name{synth_glm}
\alias{synth_glm}
\title{Pooled generalised-linear analysis of synthetic data}
\usage{
synth_glm(
  object,
  formula,
  family = stats::gaussian(),
  rule = c("synthpop", "reiter"),
  population_inference = TRUE,
  conf.level = 0.95,
  ...
)
}
\arguments{
\item{object}{A \code{synth_result} from \code{\link[=synth]{synth()}}.}

\item{formula}{A model formula (e.g. \code{y ~ x1 + x2}).}

\item{family}{A \code{\link[=family]{family()}} for \code{\link[=glm]{glm()}}; default \code{\link[=gaussian]{gaussian()}} (linear model).}

\item{rule, population_inference, conf.level}{Passed to \code{\link[=pool_synth]{pool_synth()}}.}

\item{...}{Further arguments to \code{\link[=glm]{glm()}}.}
}
\value{
A \code{flexsynth_pool} object (see \code{\link[=pool_synth]{pool_synth()}}).
}
\description{
Convenience wrapper over \code{\link[=pool_synth]{pool_synth()}} for the common case: fit the same
\code{\link[=glm]{glm()}} on each synthetic dataset and combine. With the default
\code{family = gaussian()} this is an ordinary linear model.
}
\examples{
d <- data.frame(id = 1:400, x = rnorm(400))
d$y <- rbinom(400, 1, plogis(-0.5 + d$x))
res <- synth(d, ~ id, m = 5, seed = 1)
synth_glm(res, y ~ x, family = binomial())
}
