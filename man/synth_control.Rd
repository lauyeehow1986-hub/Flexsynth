% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/synth-control.R
\name{synth_control}
\alias{synth_control}
\title{Tuning controls for synthesis}
\usage{
synth_control(
  visit_sequence = NULL,
  predictor_matrix = NULL,
  method = NULL,
  smoothing = NULL,
  proper = FALSE,
  k = NULL,
  cart = list(),
  forest = list(),
  constraint_max_tries = 50L,
  parallel = FALSE
)
}
\arguments{
\item{visit_sequence}{Optional character vector giving the order in which
variables are synthesised. \code{NULL} uses the natural (left-to-right) order.}

\item{predictor_matrix}{Optional 0/1 matrix marking, for each variable, which
variables may predict it (including cross-table predictors). \code{NULL} lets
the engine derive a sensible default.}

\item{method}{Optional per-variable method override; a single string applies
globally. \code{NULL} uses the call-level \code{method}.}

\item{smoothing}{Numeric-variable smoothing: \code{NULL} (none), \code{TRUE} /
\code{"density"} (kernel-smooth every numeric draw), or a character vector of
variable names to smooth.}

\item{proper}{Logical; use proper (\code{TRUE}) vs improper (\code{FALSE}) synthesis.}

\item{k}{Optional size of each synthetic dataset. \code{NULL} matches the input.}

\item{cart, forest}{Named lists of hyperparameters passed to the CART / random
forest backends. For \code{forest}, \code{ntree} (number of trees, default 10) and
\code{mtry} (predictors tried per tree, default all) are recognised.}

\item{constraint_max_tries}{Integer; how many times \code{\link[=synth]{synth()}} may regenerate
while rejection-sampling to satisfy \code{constraints}. Defaults to 50.}

\item{parallel}{Controls parallel generation of the \code{m} synthetic replicates.
\code{FALSE} (default) synthesises serially. \code{TRUE} uses all available workers
(\code{getOption("mc.cores")}, else \code{\link[parallel:detectCores]{parallel::detectCores()}}); a positive
integer sets an explicit worker count. Parallelism spreads the independent
replicates over a cluster, so it only helps when \code{m > 1}. A parallel run is
reproducible for a fixed (\code{seed}, worker count) via independent L'Ecuyer
RNG streams, but its output differs from a serial run with the same seed.}
}
\value{
An object of class \code{synth_control} (a validated list).
}
\description{
Collects the fine-grained knobs for the sequential synthesis engine. Strong
defaults mean beginners can ignore this; power users can tune the
utility / privacy / speed trade-off. Returns a validated \code{synth_control}
object consumed by \code{\link[=synth]{synth()}} and \code{\link[=synth_linked]{synth_linked()}}.
}
\examples{
ctrl <- synth_control(proper = TRUE, cart = list(minbucket = 5))
ctrl
}
