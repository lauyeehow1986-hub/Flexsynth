% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/disclosure.R
\name{disclosure_risk}
\alias{disclosure_risk}
\title{Empirical disclosure-risk diagnostics for synthetic data}
\usage{
disclosure_risk(
  real,
  syn,
  quasi = NULL,
  holdout = NULL,
  max_records = 2000L,
  seed = NULL,
  ...
)
}
\arguments{
\item{real}{The real \code{data.frame} (or named list) the data was synthesised
from.}

\item{syn}{The synthetic data: a \code{data.frame}, \code{\link[=synth]{synth()}} \code{synth_result},
named list, or \code{\link[=synth_linked]{synth_linked()}} \code{synth_linked_result}.}

\item{quasi}{Character vector of quasi-identifier columns; defaults to all
columns present in both frames.}

\item{holdout}{Optional \code{data.frame} of real records \emph{excluded} from
synthesis, enabling the membership-inference check.}

\item{max_records}{Cap on the number of rows used for the distance
computations (each of real / synthetic is sampled down to this); keeps the
pairwise distances tractable. Default 2000.}

\item{seed}{Optional integer seed for the subsampling.}

\item{...}{Unused.}
}
\value{
A \code{flexsynth_disclosure} object (or \code{flexsynth_disclosure_list}) with
a \code{print()} method.
}
\description{
Reports how much a synthetic dataset could leak about the real records it was
trained on. Three complementary measures are computed:
}
\details{
\itemize{
\item \strong{Replicated uniques} — records that are unique in the real data on
the quasi-identifiers and are nonetheless reproduced exactly in the
synthetic data. These are the classic identity-disclosure risks.
\item \strong{Distance to closest record (DCR)} — for each synthetic record, the
Gower distance to the nearest real record. A DCR of 0 is an exact copy.
The synthetic-to-real distances are compared against the real-to-real
nearest-neighbour distances: if synthetic records are not systematically
closer to real records than real records are to each other, identity risk
is low.
\item \strong{Membership inference} — if a \code{holdout} of records \emph{not} used for
synthesis is supplied, an attacker who guesses "member" for records close
to the synthetic data is simulated. The reported AUC (0.5 = no advantage)
and advantage (\code{2 * AUC - 1}) measure how well training membership can be
inferred.
}

Quasi-identifiers should be the genuinely identifying columns; exclude
surrogate keys such as a regenerated unit id (a synthetic id never matches a
real one, which would understate risk). If \code{real} and \code{syn} are named lists
(or \code{syn} is a \code{synth_linked_result}), each table is assessed separately.
}
\examples{
df <- data.frame(
  id  = 1:200,
  age = round(rnorm(200, 60, 10)),
  sex = sample(c("F", "M"), 200, replace = TRUE)
)
res <- synth(df, ~ id, seed = 1)
disclosure_risk(df, res, quasi = c("age", "sex"))
}
\seealso{
\code{\link[=diagnose]{diagnose()}} for utility.
}
