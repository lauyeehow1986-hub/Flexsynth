% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/synth.R
\name{synth}
\alias{synth}
\title{Synthesise a single (optionally nested / longitudinal) table}
\usage{
synth(
  data,
  structure,
  method = "cart",
  constraints = NULL,
  tuning = synth_control(),
  privacy = NULL,
  m = 1,
  seed = NULL,
  ...
)
}
\arguments{
\item{data}{A \code{data.frame} in long format.}

\item{structure}{A one-sided formula giving the nesting hierarchy, e.g.
\code{~ id / visit}. The first term is the unit identifier.}

\item{method}{Synthesis method, \code{"cart"} (default) or \code{"sample"}, applied per
variable unless overridden in \code{tuning}.}

\item{constraints}{Optional \code{\link[=rule]{rule()}} (or list of rules) the synthetic data
must satisfy. Enforced by rejection sampling at the unit grain (see
\code{\link[=rule]{rule()}}); \code{constraint_max_tries} in \code{\link[=synth_control]{synth_control()}} bounds the effort.}

\item{tuning}{A \code{\link[=synth_control]{synth_control()}} object.}

\item{privacy}{\code{NULL} for Track A, or a \code{\link[=dp_control]{dp_control()}} object for Track B.}

\item{m}{Number of synthetic datasets to produce.}

\item{seed}{Optional integer seed for reproducibility.}

\item{...}{Reserved for future use.}
}
\value{
A \code{synth_result} object. Use \code{\link[=as.data.frame]{as.data.frame()}} (for \code{m == 1}) or
\verb{$syn} to get the synthetic data.
}
\description{
Generate synthetic data for one table, working natively in long format. The
\code{structure} formula declares the nesting hierarchy (e.g.
\code{~ id / visit / test_number}) so repeated-measures and nested designs are kept
without pivoting to wide format.
}
\details{
The default (Track A) engine is sequential conditional synthesis in the
synthpop lineage: the unit identifier is regenerated by resampling whole
units (preserving realistic per-unit block sizes), and each remaining variable
is synthesised in turn from a model fitted on the real data, conditioning on
the structural columns and everything synthesised before it. With
\code{method = "cart"} each draw comes from the matching leaf of a regression /
classification tree, so marginal and conditional distributions are preserved
without distributional assumptions. Track A carries \strong{no} formal privacy
guarantee.

Columns that are constant within every real unit (baseline covariates such as
age or sex) are detected automatically and synthesised \strong{once per unit}, then
broadcast across that unit's rows, so a synthetic subject stays internally
consistent. Time-varying columns are synthesised with an initial-state model
(first row of each unit) plus a Markov transition model that conditions on the
previous row within the unit (lag-1 predictors), so autocorrelation across
visits is preserved. The number of rows per unit is drawn from the learned
count distribution and the structural-index sequence is regenerated for each
synthetic unit.

Any column containing \code{NA}s is given a missingness model: a companion
indicator is synthesised in sequence just before the column, the column's own
value model is fitted on the observed rows only, and the synthesised indicator
decides which rows are set back to \code{NA}. This preserves the missingness rate
and its association with the other variables, without letting missingness
cascade through predictors. The indicators are not returned.

Supplying \code{constraints} (see \code{\link[=rule]{rule()}}) keeps only synthetic units whose rows
satisfy every rule, regenerating until enough valid units are collected — so
logical and within-unit temporal constraints are honoured without breaking
the nested structure.

Passing \code{privacy = dp_control(...)} selects differentially private synthesis
(Track B) with a formal (\eqn{\epsilon}, \eqn{\delta}) guarantee. A flat
\code{structure = ~ id} gives a marginal release; a nesting index
(\code{~ id / visit}) additionally engages a DP Markov model that preserves
within-unit temporal structure (length, initial state, and per-variable
transitions), for which \code{max_rows_per_person} must be set to the public
maximum rows per person. See \code{vignette("differential-privacy")}.
}
\examples{
df <- data.frame(
  id    = rep(1:20, each = 2),
  visit = rep(1:2, times = 20),
  age   = rep(round(rnorm(20, 60, 8)), each = 2),
  sbp   = round(rnorm(40, 130, 15))
)
res <- synth(df, structure = ~ id / visit, seed = 1)
head(as.data.frame(res))
}
