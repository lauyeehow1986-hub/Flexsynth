% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/synth.R
\name{synth}
\alias{synth}
\title{Synthesise a single (optionally nested / longitudinal) table}
\usage{
synth(
  data,
  structure,
  method = "cart",
  constraints = NULL,
  tuning = synth_control(),
  privacy = NULL,
  m = 1,
  seed = NULL,
  ...
)
}
\arguments{
\item{data}{A \code{data.frame} in long format.}

\item{structure}{A one-sided formula giving the nesting hierarchy, e.g.
\code{~ id / visit}.}

\item{method}{Synthesis method (e.g. \code{"cart"}), applied per variable unless
overridden in \code{tuning}.}

\item{constraints}{Optional constraints / rules (temporal, logical, range).}

\item{tuning}{A \code{\link[=synth_control]{synth_control()}} object.}

\item{privacy}{\code{NULL} for Track A, or a \code{\link[=dp_control]{dp_control()}} object for Track B.}

\item{m}{Number of synthetic datasets to produce.}

\item{seed}{Optional integer seed for reproducibility.}

\item{...}{Reserved for future use.}
}
\value{
A \code{synth_result} object (once the engine lands). See the roadmap.
}
\description{
Generate synthetic data for one table, working natively in long format. The
\code{structure} formula declares the nesting hierarchy (e.g.
\code{~ id / visit / test_number}) so repeated-measures and nested designs are
preserved without pivoting to wide format.
}
\details{
Passing \code{privacy = dp_control(...)} switches from the default high-utility
engine (Track A) to differentially private synthesis (Track B).
}
\examples{
\dontrun{
synth(my_long_df, structure = ~ id / visit, method = "cart", seed = 1)
}
}
