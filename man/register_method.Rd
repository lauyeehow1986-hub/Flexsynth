% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/methods.R
\name{register_method}
\alias{register_method}
\title{Register a synthesis method}
\usage{
register_method(
  name,
  fit,
  draw,
  numeric = TRUE,
  categorical = TRUE,
  needs_predictors = TRUE
)
}
\arguments{
\item{name}{Method name (a single string) used to select it.}

\item{fit}{A function \verb{function(y, x, control)} returning a fitted model.}

\item{draw}{A function \verb{function(model, x, n, control)} returning \code{n} values.}

\item{numeric, categorical}{Logical flags declaring which target types the
method supports (used for validation). Both default to \code{TRUE}.}

\item{needs_predictors}{Logical; if \code{TRUE} (default) the engine falls back to
marginal sampling when the variable has no predictors.}
}
\value{
Invisibly, the method name.
}
\description{
Add a custom per-variable synthesiser to the method registry so it can be
selected by name via the \code{method} argument of \code{\link[=synth]{synth()}} / \code{\link[=synth_linked]{synth_linked()}} or
the per-variable \code{method} in \code{\link[=synth_control]{synth_control()}}. Registering a name that
already exists overwrites it, so you can override a built-in.
}
\details{
A method is a pair of functions:
\describe{
\item{\code{fit(y, x, control)}}{fits a model of the target \code{y} on the predictor
\code{data.frame} \code{x} (which may have zero columns) and returns any object;}
\item{\code{draw(model, x, n, control)}}{returns a length-\code{n} vector of synthetic
values for new predictor rows \code{x}.}
}
\code{control} is the \code{\link[=synth_control]{synth_control()}} object, so a method can honour \code{proper},
read its own hyperparameters, and so on.
}
\examples{
# A trivial "mean" method for numeric variables.
register_method(
  "constant_mean",
  fit  = function(y, x, control) mean(y),
  draw = function(model, x, n, control) rep(model, n),
  categorical = FALSE
)
"constant_mean" \%in\% list_methods()
}
