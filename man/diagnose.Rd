% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diagnose.R
\name{diagnose}
\alias{diagnose}
\title{Utility diagnostics comparing synthetic data to the real data}
\usage{
diagnose(real, syn, vars = NULL, propensity = "logistic", ...)
}
\arguments{
\item{real}{The real \code{data.frame} (or a named list of them for linked data).}

\item{syn}{The synthetic data: a \code{data.frame}, a \code{\link[=synth]{synth()}} \code{synth_result}, a
named list of tables, or a \code{\link[=synth_linked]{synth_linked()}} \code{synth_linked_result}.}

\item{vars}{Optional character vector restricting the variables compared;
defaults to all columns present in both.}

\item{propensity}{Propensity model for the pMSE, \code{"logistic"} (default) or
\code{"cart"} (needs the \code{rpart} package).}

\item{...}{Unused.}
}
\value{
A \code{flexsynth_diagnostics} object (or a \code{flexsynth_diagnostics_list}
for linked / list input). Has \code{print()} and \code{plot()} methods.
}
\description{
Quantifies how faithfully synthetic data reproduces the real data's
distributions and dependence structure. Because Track A output carries no
formal privacy guarantee, these utility diagnostics (and the risk
diagnostics in \code{\link[=disclosure_risk]{disclosure_risk()}}) are how synthesis quality is judged.
}
\details{
Four views are reported:
\itemize{
\item \strong{Univariate} — per-variable marginal fit: a Kolmogorov-Smirnov
statistic for numeric variables and a total-variation distance for
categorical ones (both in \eqn{[0, 1]}; smaller is better).
\item \strong{Correlation} — the Frobenius and mean absolute difference between
the real and synthetic numeric correlation matrices.
\item \strong{Association} — the mean and maximum absolute difference between the
real and synthetic categorical association (Cramer's V) matrices, so
dependence between factor variables is checked, not just numeric ones.
\item \strong{Propensity (pMSE)} — a general utility score: a model is fitted to
tell real from synthetic records; when they are indistinguishable the
propensity mean squared error sits near its null expectation, so the
\code{ratio} is near 1 (larger means more distinguishable).
}

If \code{real} and \code{syn} are named lists (or \code{syn} is a \code{synth_linked_result}),
each table is diagnosed and a per-table result is returned.
}
\examples{
df <- data.frame(
  id  = 1:200,
  age = round(rnorm(200, 60, 10)),
  sbp = round(rnorm(200, 130, 15))
)
res <- synth(df, ~ id, seed = 1)
diagnose(df, res)
}
\seealso{
\code{\link[=disclosure_risk]{disclosure_risk()}} for privacy risk.
}
